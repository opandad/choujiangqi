#ifndef HANDLE_H
#define HANDLE_H
#include "dialog.h"
#include <QMessageBox>
#include <random>

typedef long long ll;

DWORD WINAPI adadadad(LPVOID Wparam);

Ui::Dialog *returnobject();

#endif // HANDLE_H
